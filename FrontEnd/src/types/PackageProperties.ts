export type PackageProperties = {
    //some time image can be added in to this properties
    _id:string;
    packageID: string;
    packageName: string;
    daysHrsCount: string;
    description: string;
    offers: number;
    packageAmount: number;
};